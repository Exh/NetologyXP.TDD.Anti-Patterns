"use strict";

var assert = require('chai').assert;
var Visitor = require('../src/visitor');
var CocktailBuilder = require('../tests/dsl/cocktail-builder');

suite('when drinking cocktail', function () {
    test('bar is always full of drinks - passes only on my local machine', function () {
        // ...

        var car = visitor.getMyCar("mycar.jpg");
        var isOpened = config.getLocalString("bar.isOpened");
        var userName = "dpavlov";

        // ...
    });
});